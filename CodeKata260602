#include <string>
#include <vector>

using namespace std;

string solution(string s) {
    string answer = "";
    int n = s.length();
    
    if (n % 2 == 0) {
        answer = s.substr(n / 2 - 1, 2);
    } 
    else {
        answer = s.substr(n / 2, 1);
    }
    
    return answer;
}
