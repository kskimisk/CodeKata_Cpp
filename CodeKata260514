#include <string>
#include <vector>
#include <cassert>

using namespace std;

vector<int> solution(long long n) {
	assert (n <= 10000000000);
vector<int> answer;
    
    while (n > 0) {
        answer.push_back(n % 10); 
        
        n /= 10; 
    }
    return answer;
}
